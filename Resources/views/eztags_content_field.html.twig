{% block eztags_field %}
{% spaceless %}
    {% for tag in field.value.tags %}
        <a href="{{ path( tag ) }}">{{ ez_trans_prop( tag, 'keyword' ) }}</a>{% if not loop.last %}, {% endif %}
    {% endfor %}
{% endspaceless %}
{% endblock %}
