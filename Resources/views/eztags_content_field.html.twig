{% block eztags_field %}
{% spaceless %}
    {% for tag in field.value.tags %}
        <a href="{{ path( 'eztags_tag_view_url', { 'tagUrl':  eztags_tag_url( tag ) } ) }}">{{ tag.keyword }}</a>{% if not loop.last %}, {% endif %}
    {% endfor %}
{% endspaceless %}
{% endblock %}
