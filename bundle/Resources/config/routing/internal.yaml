_eztagsTag:
    path: /tags/id/{tagId}/{viewType}/{layout}
    controller: eztags.controller.tag_view:viewAction
    defaults:
        viewType: full
        layout: true
    methods: [GET]
    requirements:
        tagId: \d+
