{% extends eztags_admin.pageLayoutTemplate %}

{% trans_default_domain 'eztags_admin' %}

{% block content %}
    <h2 class="ng-ui-title">{{ 'tag.delete.title'|trans }}: {{ netgen_tags_tag_keyword(tag) }} [{{ tag.id }}]</h2>

    <p>{{ 'tag.delete.message'|trans({'%tagKeyword%': netgen_tags_tag_keyword(tag) }) }}</p>

    <form method="post" action="{{ path('netgen_tags_admin_tag_delete', {'tagId': tag.id}) }}" class="ng-ui-actions">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('eztags_admin') }}" />
        <button type="submit" class="ng-ui-btn ng-ui-btn-primary" name="DeleteTagButton">{{ 'tag.button.yes'|trans }}</button>
        <a href="{{ path('netgen_tags_admin_tag_show', {'tagId': tag.id}) }}" class="ng-ui-btn">{{ 'tag.button.no'|trans }}</a>
    </form>

{% endblock %}
