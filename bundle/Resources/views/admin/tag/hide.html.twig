{% extends netgen_tags_admin.pageLayoutTemplate %}

{% trans_default_domain 'netgen_tags_admin' %}

{% block content %}
    <h2 class="tag-title">{{ 'tag.hide.title'|trans }}: {{ tag.keyword }} [{{ tag.id }}]</h2>

    <p>{{ 'tag.hide.message'|trans({'%tagKeyword%': tag.keyword}) }}</p>

    <form method="post" action="{{ path('netgen_tags_admin_tag_hide', {'tagId': tag.id}) }}" class="tags-actions">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('netgen_tags_admin') }}" />
        <button type="submit" class="tags-btn tags-btn-primary" name="HideTagButton">{{ 'tag.button.yes'|trans }}</button>
        <a href="{{ path('netgen_tags_admin_tag_show', {'tagId': tag.id}) }}" class="tags-btn">{{ 'tag.button.no'|trans }}</a>
    </form>
{% endblock %}
